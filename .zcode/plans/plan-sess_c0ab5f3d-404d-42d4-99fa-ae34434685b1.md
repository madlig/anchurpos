Copy file `antigravity-setup/agents-custom/diagnostician/agent.md` ke lokasi aktif `.agents/agents/diagnostician/agent.md` (double "agents" — sesuai spec Antigravity). Setelah itu Anda restart Antigravity IDE untuk discovery.

Langkah yang akan saya lakukan:
1. Buat folder `.agents/agents/diagnostician/` (jika belum ada).
2. Copy file dari staging ke lokasi aktif.
3. Verifikasi file sudah ada di lokasi aktif + cek frontmatter benar.
4. Beri Anda instruksi restart + cara verify di UI Antigravity.

Command yang akan dijalankan (read-only-ish, hanya buat folder + copy 1 file):
```bash
mkdir -p .agents/agents/diagnostician
cp antigravity-setup/agents-custom/diagnostician/agent.md .agents/agents/diagnostician/agent.md
ls -la .agents/agents/diagnostician/
```

Tidak mengubah/menghapus file lain. Tidak commit ke git.